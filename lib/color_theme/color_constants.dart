import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(0xFF007AFF); // Dark blue
  static const Color accentColor = Color(0xFF66B3FF); // Light blue
  static const Color backgroundColor = Colors.white;
  static const Color cardColor = Color.fromARGB(255, 245, 245, 245);
}
