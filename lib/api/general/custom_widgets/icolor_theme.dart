import 'package:flutter/material.dart';

class ColorTheme {
  static const Color defaultColor = Color.fromRGBO(0, 0, 0, 0);
  static Color appBar = defaultColor;
  static Color footer = defaultColor;
  static Color mainBar = defaultColor;
  static Color arrowsBar = defaultColor;
  static Color scheduleButton = defaultColor;
}
